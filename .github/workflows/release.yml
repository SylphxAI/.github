name: Release

on:
  workflow_call:
    inputs:
      working-directory:
        description: 'Working directory'
        type: string
        default: '.'
    secrets:
      NPM_TOKEN:
        required: true
      SLACK_WEBHOOK:
        required: false

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: SylphxAI/bump@v0
        id: bump
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          npm-token: ${{ secrets.NPM_TOKEN }}
          working-directory: ${{ inputs.working-directory }}

      - name: Slack notification (success)
        if: steps.bump.outputs.published == 'true' && secrets.SLACK_WEBHOOK != ''
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          VERSION: ${{ steps.bump.outputs.version }}
          VERSIONS: ${{ steps.bump.outputs.versions }}
        run: |
          REPO="${{ github.repository }}"
          REPO_URL="https://github.com/$REPO"

          if [ -n "$VERSION" ]; then
            MESSAGE="📦 *${REPO}* \`v${VERSION}\` published\n<${REPO_URL}/releases/tag/v${VERSION}|View Release>"
          elif [ -n "$VERSIONS" ] && [ "$VERSIONS" != "{}" ]; then
            MESSAGE="📦 *Packages published*\n"
            for pkg in $(echo "$VERSIONS" | jq -r 'keys[]' 2>/dev/null); do
              ver=$(echo "$VERSIONS" | jq -r --arg p "$pkg" '.[$p]' 2>/dev/null)
              MESSAGE="${MESSAGE}• ${pkg}@${ver}\n"
            done
            MESSAGE="${MESSAGE}<${REPO_URL}|View Repository>"
          else
            MESSAGE="📦 Release published from *${REPO}*\n<${REPO_URL}|View Repository>"
          fi

          curl -s -X POST "$SLACK_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "{\"text\": \"${MESSAGE}\"}"

      - name: Slack notification (failure)
        if: failure() && secrets.SLACK_WEBHOOK != ''
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
        run: |
          curl -s -X POST "$SLACK_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "{\"text\": \"❌ *Publish failed*\nRepository: ${{ github.repository }}\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Workflow>\"}"
