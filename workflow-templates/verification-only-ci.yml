# Verification-only CI — Agent-native Fast Trunk
# Canonical ADR: platform ADR-01KYTNE0B96325DDAD28FD1341
# https://github.com/SylphxAI/platform/blob/main/docs/adr/ADR-01KYTNE0B96325DDAD28FD1341-agent-native-fast-trunk-cicd.md
name: CI
on:
  pull_request:
  push:
    branches: [main]
  # No merge_group — Merge Queue off by default.
permissions:
  contents: read
concurrency:
  # PR number || ref — never github.sha (no free-runners).
  group: ci-${{ github.workflow }}-${{
    github.event.pull_request.number || github.ref
  }}
  cancel-in-progress: true
jobs:
  source-ci:
    name: source-ci/pass
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run lint
      - run: bun run typecheck
      - run: bun run test
      # Forbidden: production packaging (next/bun app build, cargo --release ship, docker deploy image)
